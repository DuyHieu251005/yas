# ==============================================================
# Authorization Policies - Kiểm soát service-to-service access
#
# NGUYÊN TẮC:
# - Chỉ đặt ALLOW policy trên BACKEND services (product, cart, order...)
# - KHÔNG đặt policy trên frontend (BFF, UI) → Istio allow all by default
# - Service nào có ALLOW policy → CHỈ traffic match mới qua được
# - Dùng source.namespace để xác thực nguồn gửi request
# ==============================================================

# product: chỉ nhận từ storefront-bff, backoffice-bff và order
apiVersion: security.istio.io/v1beta1
kind: AuthorizationPolicy
metadata:
  name: allow-to-product
  namespace: yas-app
spec:
  action: ALLOW
  selector:
    matchLabels:
      app: product
  rules:
    - from:
        - source:
            namespaces: ["yas-app"]
            principals:
              - "cluster.local/ns/yas-app/sa/storefront-bff"
              - "cluster.local/ns/yas-app/sa/backoffice-bff"
              - "cluster.local/ns/yas-app/sa/order"
              - "cluster.local/ns/yas-app/sa/cart"
              - "cluster.local/ns/yas-app/sa/inventory"
              - "cluster.local/ns/yas-app/sa/promotion"
---
# cart: chỉ nhận từ storefront-bff
apiVersion: security.istio.io/v1beta1
kind: AuthorizationPolicy
metadata:
  name: allow-to-cart
  namespace: yas-app
spec:
  action: ALLOW
  selector:
    matchLabels:
      app: cart
  rules:
    - from:
        - source:
            namespaces: ["yas-app"]
            principals:
              - "cluster.local/ns/yas-app/sa/storefront-bff"
              - "cluster.local/ns/yas-app/sa/order"
---
# order: chỉ nhận từ storefront-bff
apiVersion: security.istio.io/v1beta1
kind: AuthorizationPolicy
metadata:
  name: allow-to-order
  namespace: yas-app
spec:
  action: ALLOW
  selector:
    matchLabels:
      app: order
  rules:
    - from:
        - source:
            namespaces: ["yas-app"]
            principals:
              - "cluster.local/ns/yas-app/sa/storefront-bff"
              - "cluster.local/ns/yas-app/sa/payment"
              - "cluster.local/ns/yas-app/sa/rating"
---
# customer: nhận từ storefront-bff và backoffice-bff
apiVersion: security.istio.io/v1beta1
kind: AuthorizationPolicy
metadata:
  name: allow-to-customer
  namespace: yas-app
spec:
  action: ALLOW
  selector:
    matchLabels:
      app: customer
  rules:
    - from:
        - source:
            namespaces: ["yas-app"]
            principals:
              - "cluster.local/ns/yas-app/sa/storefront-bff"
              - "cluster.local/ns/yas-app/sa/backoffice-bff"
              - "cluster.local/ns/yas-app/sa/order"
              - "cluster.local/ns/yas-app/sa/rating"
---
# inventory: nhận từ order và backoffice-bff
apiVersion: security.istio.io/v1beta1
kind: AuthorizationPolicy
metadata:
  name: allow-to-inventory
  namespace: yas-app
spec:
  action: ALLOW
  selector:
    matchLabels:
      app: inventory
  rules:
    - from:
        - source:
            namespaces: ["yas-app"]
            principals:
              - "cluster.local/ns/yas-app/sa/order"
              - "cluster.local/ns/yas-app/sa/backoffice-bff"
---
# tax: chỉ nhận từ order
apiVersion: security.istio.io/v1beta1
kind: AuthorizationPolicy
metadata:
  name: allow-to-tax
  namespace: yas-app
spec:
  action: ALLOW
  selector:
    matchLabels:
      app: tax
  rules:
    - from:
        - source:
            namespaces: ["yas-app"]
            principals:
              - "cluster.local/ns/yas-app/sa/order"
---
# search: chỉ nhận từ storefront-bff
apiVersion: security.istio.io/v1beta1
kind: AuthorizationPolicy
metadata:
  name: allow-to-search
  namespace: yas-app
spec:
  action: ALLOW
  selector:
    matchLabels:
      app: search
  rules:
    - from:
        - source:
            namespaces: ["yas-app"]
            principals:
              - "cluster.local/ns/yas-app/sa/storefront-bff"
---
# media: nhận từ backoffice-bff, storefront-bff và product
apiVersion: security.istio.io/v1beta1
kind: AuthorizationPolicy
metadata:
  name: allow-to-media
  namespace: yas-app
spec:
  action: ALLOW
  selector:
    matchLabels:
      app: media
  rules:
    - from:
        - source:
            namespaces: ["yas-app"]
            principals:
              - "cluster.local/ns/yas-app/sa/backoffice-bff"
              - "cluster.local/ns/yas-app/sa/storefront-bff"
              - "cluster.local/ns/yas-app/sa/product"
              - "cluster.local/ns/yas-app/sa/payment"
